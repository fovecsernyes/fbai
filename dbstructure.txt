Bird:
	- ID serial
	- neuralis halo
	- cycleID (foreign key cycle.parentID)
Cycle:
	- ID serial (primary key )
	- futasi parameterek
	- parent ID (cycle.ID-ra)
	- sum fitness

Fitness:
	- ID serial(primary key)
	- fitness
	- bird ID (foreign key bird.ID)
	- cycle ID (foreign key cycle.ID)


tablak letrehozunk (ha nincs)
for i in rounds: (post js->py)
	Cycle instert(default, POST-on jott, onmaga, 0)
	if 0 == select count(*) from bird where cycleID == cycle.parentID
		for i in population:
			Bird instert(currentID, neuralishalo)
	(post py->js)
	js run
	(js finished: post js->py)
	receive post(array[birdID ,fitness])
	for i in populaton:
		fitness instert(default,fitness, birdID, cycleID)
	cycle.update(sum fitness)
	(post py->js)

